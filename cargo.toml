[package]
name = "mymodel"
version = "0.1.0"
edition = "2021"

[lib]
name = "mymodel"
crate-type = ["cdylib"]

[dependencies]
image = "0.24"
walkdir = "2.4"
csv = "1.3"
rand = "0.8"
nalgebra = "0.32"

# ndarray = "0.15"
# ndarray-linalg = { version = "0.16", features = ["intel-mkl-static"] }

[package.metadata.vcpkg]
dependencies = ["openblas"]
